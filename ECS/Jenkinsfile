pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Start Building Dockerfile........'
                sh 'docker build -f ECS/mynodeapp/Dockerfile -t lakshithah1/mynodeapp ECS/mynodeapp'
            }
        }
        stage('Test') {
            steps {
                echo 'Testing..'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying....'
            }
        }
    }
}
